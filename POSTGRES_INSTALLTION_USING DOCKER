
sudo docker run --name some-postgres -p 5432:5432  -e POSTGRES_PASSWORD=mysecretpassword -d postgres

sudo apt install postgresql-client

psql -h localhost -p 5432 -U postgres

 psql -h localhost -p 5432 -U postgres
     Password for user postgres: "give passwd "


Inside postges command;
-----------------------
>> select version ();
>> create table foss(name text, id int generated by default as identity);
>> select * from foss;
>> insert into foss(name) values('postgres'),('Lucee'),('DBeaver');
>> select * from foss;
>> \q

sudo docker container exec -it pg bash

sudo exec -it subbu psql -U postgres

sudo docker exec -it subbu psql -U postgres
{------------------------------------------------------------------------------------------------------------}
{------------------------------------------------------------------------------------------------------------}

sudo docker run --name postgresdb -p 5432:5432 -e POSTGRES_PASSWORD=mypostgres -v pgdata  postgres:15.2

{---------------------------------------------------------------------------------------------------------}
{.........................................................................................................}

sudo docker run -d --name subbu -p 5432:5432 -e POSTGRES_PASSWORD=secret -v database postgres:14.2

sudo docker run -d --name subbu -p 5432:5432 -e POSTGRES_PASSWORD=secret -e PGDATA=/pgdata -v /pgdata:/pgdata  postgres:14.2

sudo ls -l /pgdata/

{-------------------------------------------------------------------------------------------------------------------------------------}
{-------------------------------------------------------------------------------------------------------------------------------------}


